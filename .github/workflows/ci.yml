jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build and test
      id: build-image
      env:
        CI_VERBOSE: 1
      run: |
        make -f Makefile.ci
